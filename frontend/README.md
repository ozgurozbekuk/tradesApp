# Frontend

React + Vite client for TradesApp.

## Environment
Copy `.env.example` to `.env`.

- `VITE_CLERK_PUBLISHABLE_KEY`
- `VITE_API_BASE_URL` defaults to `http://localhost:3000`

## Development
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```
